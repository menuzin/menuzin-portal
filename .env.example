# Database
DATABASE_URL="postgresql://user:password@localhost:5432/database?schema=public"

# Admin Setup (for initial admin account creation)
# Remove this after first setup, or set it in your deployment environment
ADMIN_SETUP_PIN=123456

# Optional: JWT Secret (if needed in future)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-chars"

# Site URL (optional, for production)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Optional: Supabase (for image uploads)
# NEXT_PUBLIC_SUPABASE_URL="your-supabase-url"
# NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"
